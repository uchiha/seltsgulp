import {Given,  Then}  from 'cucumber';

Given('that something happens with gulp', () => {
    console.log('->>> yep, it did!');
});

Given('that it reaally happend with gulp', () => {
    console.log('like, reeaaalllly!');
});